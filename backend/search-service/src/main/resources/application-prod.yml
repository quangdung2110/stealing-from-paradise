# ============================================================
# PROD Profile — search-service
# Active when SPRING_PROFILES_ACTIVE=prod
# ============================================================
logging:
  level:
    root: INFO
    com.flashsale: INFO
    org.springframework.data.elasticsearch: WARN
    org.elasticsearch: WARN
    org.apache.kafka: WARN
    org.springframework.kafka: WARN
    kafka: WARN

dev-data:
  enabled: false
