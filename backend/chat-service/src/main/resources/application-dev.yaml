# ============================================================
# DEV Profile — chat-service
# Active when SPRING_PROFILES_ACTIVE=dev
# ============================================================
logging:
  level:
    com.flashsale: DEBUG
    org.springframework.data.mongodb: INFO

dev-data:
  enabled: true
  # reset=true wipes chat_sessions + chat_messages before reseeding.
  reset: ${DEV_DATA_RESET:false}
