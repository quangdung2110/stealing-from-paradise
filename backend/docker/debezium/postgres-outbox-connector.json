{
  "name": "postgres-outbox-connector",
  "config": {
    "connector.class": "io.debezium.connector.postgresql.PostgresConnector",
    "tasks.max": "1",
    "plugin.name": "pgoutput",
    "database.hostname": "${env:CDC_DB_HOST}",
    "database.port": "${env:CDC_DB_PORT}",
    "database.user": "${env:CDC_DB_USER}",
    "database.password": "${env:CDC_DB_PASSWORD}",
    "database.dbname": "${env:CDC_DB_NAME}",
    "database.server.name": "${env:CDC_DB_SERVER_NAME}",
    "schema.include.list": "orders,payment,refund",
    "table.include.list": "orders.outbox_event,payment.outbox_event,refund.outbox_event",
    "topic.prefix": "${env:CDC_TOPIC_PREFIX}",
    "tombstones.on.delete": "false",
    
    "transforms": "outbox",
    "transforms.outbox.type": "io.debezium.transforms.outbox.EventRouter",
    "transforms.outbox.route.by.field": "topic",
    "transforms.outbox.route.topic.replacement": "${routedByValue}",
    "transforms.outbox.table.field.event.key": "msg_key",
    "transforms.outbox.table.field.event.payload": "payload",
    "transforms.outbox.table.expand.json.payload": "true"
  }
}
